name: manage_calendar_events
description: A flutter plugin which will help you add, edit and remove  the events from your (Android&#x2F;ios) device calendar
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The androidPackage and pluginClass identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    androidPackage: in.fantastic.manage_calendar_events
    pluginClass: ManageCalendarEventsPlugin

